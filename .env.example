# Docker代理服务环境变量配置示例
# 复制此文件为.env并填写实际值

# Shadowsocks代理服务配置

# 基础配置
SERVER_PORT=8388
PASSWORD=your_password  # 请修改为强密码
METHOD=aes-256-gcm      # 加密方式：aes-256-gcm, chacha20-ietf-poly1305等
TIMEOUT=300             # 连接超时时间（秒）
DNS_SERVER=8.8.8.8      # DNS服务器
UDPSUPPORT=true         # 是否支持UDP
LOG_LEVEL=info          # 日志级别：debug, info, warn, error

# 高级配置（可选）
# 以下配置可以在start.sh脚本中取消注释启用
# FAST_OPEN=false        # TCP Fast Open
# NO_DELAY=true          # TCP_NODELAY
# REUSE_PORT=true        # SO_REUSEPORT

# AWS/ECS配置（可选）
AWS_REGION=us-west-2
ECS_CLUSTER=proxy-cluster
ECS_SERVICE=shadowsocks-proxy-service